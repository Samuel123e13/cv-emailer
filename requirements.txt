# Optional extras. The app runs without them, but they enable nice-to-haves:
keyring>=24      # store your email password in Windows Credential Manager
openpyxl>=3.1    # import recipients from .xlsx Excel files (CSV works without it)
